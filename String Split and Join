def split_and_join(line):
    # Split the string by space and join using hyphen
    return "-".join(line.split(" "))

if __name__ == "__main__":
    line = input().strip()
    result = split_and_join(line)
    print(result)
